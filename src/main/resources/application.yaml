server:
  port: 8085
spring:
  application:
    name: book-my-ticket

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://database-1.czqsqu2o243l.ap-south-1.rds.amazonaws.com:3306/book-my-ticket?createDatabaseIfNotExist=true
    username: admin
    password: admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    # ❌ DO NOT SET OLD DIALECT (REMOVE COMPLETELY)
    # Hibernate 7 auto-detects dialect

    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      url: redis://localhost:6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: kusumacr19@gmail.com
    password: geylgfuvsnnrhwsj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

admin:
  email: admin@gmail.com
  password: admin

cloudinary:
  url: ${CLOUDINARY_URL:cloudinary://123456789012345:dummy_secret@dummy_cloud}
